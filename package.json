{
  "name": "rsi-signal-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check client.js"
  },
  "engines": {
    "node": ">=20"
  }
}
